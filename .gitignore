__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
.env
*.db
# Marker written next to the database when the dashboard seeds the bundled
# sample recording, so it can label the data as sample rather than live.
*.db.sample

# Coverage
.coverage
coverage.xml
htmlcov/

.DS_Store
